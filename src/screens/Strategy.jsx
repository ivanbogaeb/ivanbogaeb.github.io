import React, { useEffect } from 'react'

import { gsap } from 'gsap'
import { ScrollTrigger } from 'gsap/all'

const Strategy = () => {

    useEffect(() => {
    });

    return (
      <article id="strategy" style={{
          height: 'calc(100vh - 68px - 100px)'
      }}>
          Strategy
      </article>
    )
}

export default Strategy;